# psatool-busybar-agent Docs Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up a Docusaurus docs site under `website/`, themed and structured like `busybar-dotnet`'s site, with five hand written guide pages and a (currently non-runnable) Cloudflare Pages deploy workflow.

**Architecture:** A Docusaurus 3 classic-preset site (`website/`) with one docs sidebar (no API reference), a red/orange/yellow three-bar logo mark reusing this app's own priority colors, and the intro page set as the homepage via `slug: /`. Content is built up one page per task so every task ends in a working `yarn build`.

**Tech Stack:** Docusaurus 3.10.2 (React 19, TypeScript), Yarn via Corepack, Node >=20.

## Global Constraints

- No em dashes ("—") anywhere in any `.mdx`/`.md` content written for this site. Use commas, colons, parentheses, or separate sentences instead.
- Full design spec: `docs/superpowers/specs/2026-08-05-docs-site-design.md`. Follow it for anything this plan doesn't spell out verbatim.
- `organizationName: 'homotechsual'`, `projectName: 'psatool-busybar-agent'`, production `url: 'https://psatool-busybar-agent.homotechsual.dev'`.
- Priority colors, reused from `src/PsaToolAgent/Display/BusyBarRenderer.cs`: red `#FF0000` (rank 1 / critical trigger), orange `#FFA500` (rank 2), yellow `#FFFF00` (rank 3). Navbar background `#263141` (used as the favicon's backdrop only, per the existing busybar-dotnet convention that the navbar logo has no backdrop but the favicon does).
- This environment's Bash tool has an intermittent Cygwin fork issue (`cygheap read copy failed`). If a `yarn`/`git`/`corepack` command via Bash fails with that error, retry the same command using the PowerShell tool instead.
- All `yarn` commands run with working directory `website/` (inside the `psatool-busybar-agent` repo root).
- No unit tests exist for a Docusaurus site. Each task's verification is `yarn build` succeeding (Docusaurus's `onBrokenLinks: 'throw'` fails the build on any broken internal link or sidebar reference) plus checking the expected HTML file exists under `website/build/`.

---

### Task 1: Website scaffold, theme, and the Getting Started homepage

**Files:**
- Create: `website/package.json`
- Create: `website/tsconfig.json`
- Create: `website/.yarnrc.yml`
- Create: `website/.gitignore`
- Create: `website/docusaurus.config.ts`
- Create: `website/sidebars.ts`
- Create: `website/src/css/custom.css`
- Create: `website/static/img/logo.svg`
- Create: `website/static/img/favicon.svg`
- Create: `website/docs/intro.mdx`

**Interfaces:**
- Produces: a buildable Docusaurus site at route `/` (the intro page, via `slug: /`), `sidebars.ts` exporting `docsSidebar: ['intro']`, and `docusaurus.config.ts`'s `themeConfig.footer.links` with a `'Docs'` group containing only a `'Getting Started'` entry and a `'More'` group containing only a `'GitHub'` entry. Tasks 2 through 5 each add one entry to both the `docsSidebar` array and the `'Docs'` footer group, and edit `intro.mdx`'s content, so match these two arrays and this file's content exactly, character for character, so later diffs apply cleanly.

- [ ] **Step 1: Create `website/package.json`**

```json
{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "packageManager": "yarn@4.16.0+sha512.5374c94eb4ef6aa8188fb112f20c1aa6569f248d676c5e576e1fd2a1a4d8d87a96df65d9dfe1c2a0252cbe38bda46cf18d955005b81b43cc7607a5c9d56fd2b6",
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.2",
    "@docusaurus/faster": "3.10.2",
    "@docusaurus/preset-classic": "3.10.2",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.10.2",
    "@docusaurus/tsconfig": "3.10.2",
    "@docusaurus/types": "3.10.2",
    "@types/react": "^19.0.0",
    "typescript": "~6.0.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
```

- [ ] **Step 2: Create `website/tsconfig.json`**

```json
{
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "baseUrl": ".",
    "ignoreDeprecations": "6.0",
    "strict": true
  },
  "exclude": [".docusaurus", "build"]
}
```

- [ ] **Step 3: Create `website/.yarnrc.yml`**

```yaml
nodeLinker: node-modules
enableScripts: true
```

- [ ] **Step 4: Create `website/.gitignore`**

```
# Dependencies
/node_modules

# Yarn Berry (node-modules linker): cache/state only, not the lockfile
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
```

- [ ] **Step 5: Create `website/docusaurus.config.ts`**

```ts
import {themes as prismThemes} from 'prism-react-renderer';
import type {Config} from '@docusaurus/types';
import type * as Preset from '@docusaurus/preset-classic';
import {DOCUSAURUS_VERSION} from '@docusaurus/utils';

// This runs in Node.js - Don't use client-side code here (browser APIs, JSX...)

const config: Config = {
  title: 'PSA BusyBar Agent',
  tagline: 'A priority-ranked helpdesk dashboard for the BUSY Bar, driven by your PSA',
  favicon: 'img/favicon.svg',

  future: {
    v4: true, // Improve compatibility with the upcoming Docusaurus v4
  },

  url: 'https://psatool-busybar-agent.homotechsual.dev',
  baseUrl: '/',

  organizationName: 'homotechsual',
  projectName: 'psatool-busybar-agent',

  onBrokenLinks: 'throw',

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  presets: [
    [
      'classic',
      {
        docs: {
          sidebarPath: './sidebars.ts',
          routeBasePath: '/',
          editUrl: 'https://github.com/homotechsual/psatool-busybar-agent/tree/main/website/',
        },
        blog: false,
        theme: {
          customCss: './src/css/custom.css',
        },
      } satisfies Preset.Options,
    ],
  ],

  themeConfig: {
    colorMode: {
      defaultMode: 'dark',
      disableSwitch: false,
      respectPrefersColorScheme: true,
    },
    navbar: {
      title: 'PSA BusyBar Agent',
      logo: {
        alt: 'PSA BusyBar Agent Logo',
        src: 'img/logo.svg',
      },
      items: [
        {
          type: 'docSidebar',
          sidebarId: 'docsSidebar',
          position: 'left',
          label: 'Docs',
        },
        {
          href: 'https://github.com/homotechsual/psatool-busybar-agent',
          label: 'GitHub',
          position: 'right',
          target: '_blank',
          className: 'github-link',
        },
      ],
    },
    footer: {
      style: 'dark',
      links: [
        {
          title: 'Docs',
          items: [
            {label: 'Getting Started', to: '/'},
          ],
        },
        {
          title: 'More',
          items: [
            {label: 'GitHub', href: 'https://github.com/homotechsual/psatool-busybar-agent'},
          ],
        },
      ],
      copyright: `Copyright © ${new Date().getFullYear()} MJCO.<br />Built with <a href="https://docusaurus.io">Docusaurus v${DOCUSAURUS_VERSION}</a>.<br /><span class="designedBy">Designed with <svg xmlns="http://www.w3.org/2000/svg" class="heart" width="24" height="24" viewBox="0 0 24 24"><path d="M14 20.408c-.492.308-.903.546-1.192.709-.153.086-.308.17-.463.252h-.002a.75.75 0 01-.686 0 16.709 16.709 0 01-.465-.252 31.147 31.147 0 01-4.803-3.34C3.8 15.572 1 12.331 1 8.513 1 5.052 3.829 2.5 6.736 2.5 9.03 2.5 10.881 3.726 12 5.605 13.12 3.726 14.97 2.5 17.264 2.5 20.17 2.5 23 5.052 23 8.514c0 3.818-2.801 7.06-5.389 9.262A31.146 31.146 0 0114 20.408z"/></svg> by <a href="https://homotechsual.dev">homotechsual</a></span>`,
    },
    prism: {
      theme: prismThemes.github,
      darkTheme: prismThemes.dracula,
    },
  } satisfies Preset.ThemeConfig,
};

export default config;
```

- [ ] **Step 6: Create `website/sidebars.ts`**

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docsSidebar: ['intro'],
};

export default sidebars;
```

- [ ] **Step 7: Create `website/src/css/custom.css`**

```css
/**
 * Any CSS included here will be global. The classic template
 * bundles Infima by default. Infima is a CSS framework designed to
 * work well for content-centric websites.
 *
 * Palette pulled from the homotechsual site family for visual
 * consistency across projects: a warm red/terracotta primary, rounded
 * corners, and a dark navbar/footer.
 */

:root {
  --ifm-background-color: #fcfcfc;
  --ifm-button-border-radius: 0.5rem;
  --ifm-code-font-size: 95%;
  --ifm-color-danger: #b2322b;
  --ifm-color-info: #006aba;
  --ifm-color-primary: #a42828;
  --ifm-color-primary-dark: #9c2626;
  --ifm-color-primary-darker: #8b2222;
  --ifm-color-primary-darkest: #731c1c;
  --ifm-color-primary-light: #ac2a2a;
  --ifm-color-primary-lighter: #bd2e2e;
  --ifm-color-primary-lightest: #cf3a3a;
  --ifm-color-warning-dark: #e0ca06;
  --ifm-font-color-base: #333333;
  --ifm-font-color-base-inverse: #fcfcfc;
  --ifm-footer-link-hover-color: #f8dbd8;
  --ifm-global-radius: 0.5rem;
  --ifm-heading-color: #333333;
  --ifm-link-decoration: none;
  --ifm-link-hover-decoration: underline;
  --ifm-navbar-background-color: #263141;
  --ifm-navbar-color: #f2bab6;
  --ifm-navbar-link-color: #f2bab6;
  --ifm-navbar-link-hover-color: #f8dbd8;
  --ifm-navbar-shadow: 0 0 0.5rem rgba(0, 0, 0, 0.5);
  --docusaurus-highlighted-code-line-bg: rgba(0, 0, 0, 0.1);
}

/* For readability concerns, you should choose a lighter palette in dark mode. */
[data-theme='dark'] {
  --ifm-background-color: #333333;
  --ifm-color-primary: #efaaa5;
  --ifm-color-primary-dark: #ec9a94;
  --ifm-color-primary-darker: #e98a83;
  --ifm-color-primary-darkest: #e67972;
  --ifm-color-primary-light: #f2bab6;
  --ifm-color-primary-lighter: #f5cac7;
  --ifm-color-primary-lightest: #f8dbd8;
  --ifm-font-color-base: #fcfcfc;
  --ifm-font-color-base-inverse: #333333;
  --ifm-heading-color: #fcfcfc;
  --docusaurus-highlighted-code-line-bg: rgba(0, 0, 0, 0.3);
}

footer {
  --ifm-footer-background-color: var(--ifm-navbar-background-color);
  --ifm-footer-color: var(--ifm-navbar-color);
  --ifm-footer-link-color: var(--ifm-navbar-link-color);
  --ifm-footer-link-hover-color: var(--ifm-color-primary);
}

.docusaurus-highlight-code-line {
  background-color: rgba(0, 0, 0, 0.1);
  display: block;
  margin: 0 calc(-1 * var(--ifm-pre-padding));
  padding: 0 var(--ifm-pre-padding);
}

[data-theme='dark'] .docusaurus-highlight-code-line {
  background-color: rgba(0, 0, 0, 0.3);
}

@keyframes heart {
  0%, 40%, 80%, 100% {
    transform: scale(1);
  }
  20%, 60% {
    transform: scale(1.15);
  }
}

.designedBy {
  display: inline-flex;
  align-items: center;
}

.designedBy .heart {
  margin: 0.25rem;
  animation: heart 1500ms infinite;
  fill: red;
}

.designedBy a {
  margin-inline-start: 0.25rem;
}

.github-link {
  align-items: center;
  display: flex;
}

.github-link:before {
  align-self: center;
  background: url("data:image/svg+xml;base64,") 0 0/contain;
  content: '';
  display: inline-flex;
  height: 24px;
  width: 24px;
  margin-right: 0.5rem;
  color: var(--ifm-navbar-link-color);
}

.github-link:hover:before {
  background: url("data:image/svg+xml;base64,") 0 0/contain;
}
```

- [ ] **Step 8: Create `website/static/img/logo.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32" role="img" aria-label="psatool-busybar-agent logo">
  <rect x="4" y="4" width="24" height="6" rx="1.5" fill="#FF0000" />
  <rect x="4" y="13" width="24" height="6" rx="1.5" fill="#FFA500" />
  <rect x="4" y="22" width="24" height="6" rx="1.5" fill="#FFFF00" />
</svg>
```

- [ ] **Step 9: Create `website/static/img/favicon.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32" role="img" aria-label="psatool-busybar-agent favicon">
  <rect width="32" height="32" rx="4" fill="#263141" />
  <rect x="4" y="4" width="24" height="6" rx="1.5" fill="#FF0000" />
  <rect x="4" y="13" width="24" height="6" rx="1.5" fill="#FFA500" />
  <rect x="4" y="22" width="24" height="6" rx="1.5" fill="#FFFF00" />
</svg>
```

- [ ] **Step 10: Create `website/docs/intro.mdx`**

```mdx
---
sidebar_position: 1
slug: /
---

# Getting started

**PSA BusyBar Agent** polls a PSA (Halo first, others pluggable) on a timer, works out the current
helpdesk priority, and renders the result to a physical BUSY Bar over the network.

```
IPsaDataProvider -> PsaSnapshot -> PriorityEngine -> DashboardState -> BusyBarRenderer
```

## Quick start

```bash
git clone https://github.com/homotechsual/psatool-busybar-agent.git
cd psatool-busybar-agent
cp .env.example .env
```

Fill in `HALO_CLIENT_ID`, `HALO_CLIENT_SECRET`, and `HALO_BASE_URL` in `.env`, then:

```bash
docker compose up -d --build
```
```

- [ ] **Step 11: Install dependencies**

Run (PowerShell, from `website/`):

```powershell
corepack enable
yarn install
```

Expected: completes with no errors, creates `node_modules/` and `.yarn/install-state.gz`.

- [ ] **Step 12: Build and verify**

Run (PowerShell, from `website/`):

```powershell
yarn build
```

Expected: `[SUCCESS] Generated static files in "build"`, no broken link errors.

Then verify the homepage route exists:

```powershell
Test-Path build/index.html
```

Expected: `True`.

- [ ] **Step 13: Commit**

```bash
git add website/
git commit -m "Add docs site scaffold, theme, and Getting Started homepage"
```

---

### Task 2: Configuration page

**Files:**
- Create: `website/docs/configuration.mdx`
- Modify: `website/sidebars.ts`
- Modify: `website/docusaurus.config.ts`
- Modify: `website/docs/intro.mdx`

**Interfaces:**
- Consumes: `website/sidebars.ts`'s `docsSidebar: ['intro']` array and `docusaurus.config.ts`'s footer `'Docs'` group from Task 1, and `intro.mdx`'s content from Task 1 (no `## Next` section yet).
- Produces: route `/configuration`; `docsSidebar: ['intro', 'configuration']`; `intro.mdx` now has a `## Next` section (later tasks append to it, do not replace it).

- [ ] **Step 1: Create `website/docs/configuration.mdx`**

```mdx
---
sidebar_position: 2
---

# Configuration

Set configuration through `appsettings.json`, environment variables (double underscore nesting,
for example `Psa__Halo__ClientId`), or `dotnet user-secrets` for local development. Never commit
real `ClientId` or `ClientSecret` values.

| Key | Default | Description |
| --- | --- | --- |
| `Psa:Provider` | `Halo` | Which `IPsaDataProvider` implementation to use. |
| `Psa:PollIntervalSeconds` | `60` | How often to poll the PSA and refresh the display. |
| `Psa:SlaRiskThresholdMinutes` | `60` | A ticket is "SLA risk" at or below this many minutes to breach. |
| `Psa:Halo:BaseUrl` | (none) | Your Halo tenant's API base URL. |
| `Psa:Halo:ClientId` / `ClientSecret` | (none) | OAuth2 client credentials, set through environment variables or user secrets. |
| `Psa:Halo:Scope` | `read:tickets` | OAuth2 scope requested. Set this to the minimum your Halo API client is actually granted; see Least privilege on the [Deployment](/deployment) page. |
| `Psa:Halo:OrganisationId` | `1` | Halo organisation whose `portal_title` is used as the dashboard header, fetched once at startup and cached for the process lifetime. Falls back to `Dashboard:HeaderText` if the fetch fails or the organisation has no portal title set. |
| `BusyBar:Address` | `10.0.4.20` | Network address of the BUSY Bar device. |
| `Dashboard:HeaderText` | `WRC SERVICE DESK` | First line of the normal mode display, used only when the Halo organisation lookup does not produce a header. |
| `Dashboard:DisplayCycleSeconds` | `5` | How often the critical display cycles between its priority tier pages. Has no effect on normal or SLA warning mode, which each have only one page. |
```

Note: this page links to `/deployment`, which does not exist until Task 5. That is fine here:
Docusaurus only validates links found in content that is part of the current build, and by Task 5
`/deployment` will exist; `onBrokenLinks: 'throw'` re-checks the whole site on every build, so
this link will start failing the build the moment it is added if `/deployment` is missing. Add
this file's `Psa:Halo:Scope` row exactly as written above; Task 5 makes the link valid.

- [ ] **Step 2: Verify the build fails as expected before fixing it**

Run (PowerShell, from `website/`):

```powershell
yarn build
```

Expected: **fails** with a broken link error naming `/deployment` (referenced from
`configuration.mdx`, not yet a valid route). This confirms `onBrokenLinks: 'throw'` is actually
checking cross-page links, not silently passing.

- [ ] **Step 3: Temporarily point the Scope row's link at `/configuration` itself instead**

Change the `Psa:Halo:Scope` row's link target from `/deployment` to `/configuration` (a link to
its own page, which always exists) so this task's build passes standalone. Task 5 changes it back
to `/deployment` once that page exists.

```mdx
| `Psa:Halo:Scope` | `read:tickets` | OAuth2 scope requested. Set this to the minimum your Halo API client is actually granted; see Least privilege on the [Deployment](/configuration) page. |
```

- [ ] **Step 4: Modify `website/sidebars.ts`**

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docsSidebar: ['intro', 'configuration'],
};

export default sidebars;
```

- [ ] **Step 5: Modify `website/docusaurus.config.ts`'s footer**

Replace the `footer.links` array with:

```ts
      links: [
        {
          title: 'Docs',
          items: [
            {label: 'Getting Started', to: '/'},
            {label: 'Configuration', to: '/configuration'},
          ],
        },
        {
          title: 'More',
          items: [
            {label: 'GitHub', href: 'https://github.com/homotechsual/psatool-busybar-agent'},
          ],
        },
      ],
```

- [ ] **Step 6: Modify `website/docs/intro.mdx`**

Append a new section at the end of the file:

```mdx

## Next

- [Configuration](/configuration): every setting, what it defaults to, and what it does.
```

- [ ] **Step 7: Build and verify**

Run (PowerShell, from `website/`):

```powershell
yarn build
Test-Path build/configuration/index.html
```

Expected: build succeeds, `Test-Path` returns `True`.

- [ ] **Step 8: Commit**

```bash
git add website/
git commit -m "Add Configuration page"
```

---

### Task 3: Architecture page

**Files:**
- Create: `website/docs/architecture.mdx`
- Modify: `website/sidebars.ts`
- Modify: `website/docusaurus.config.ts`
- Modify: `website/docs/intro.mdx`
- Modify: `website/docs/configuration.mdx`

**Interfaces:**
- Consumes: `docsSidebar: ['intro', 'configuration']` and the footer `'Docs'` group from Task 2.
- Produces: route `/architecture`; `docsSidebar: ['intro', 'configuration', 'architecture']`.
  `/deployment` is still two tasks away, so `configuration.mdx`'s `Psa:Halo:Scope` row stays
  pointed at `/configuration` (Task 2's workaround) until Task 5 fixes it; do not touch it here.

- [ ] **Step 1: Create `website/docs/architecture.mdx`**

This page links to `/providers` twice, which does not exist until Task 4. Both links below
temporarily point at `/architecture` (this page itself, always a valid target) so this task's
build passes standalone; Task 4 corrects both once `providers.mdx` exists.

```mdx
---
sidebar_position: 3
---

# Architecture

```
IPsaDataProvider.GetSnapshotAsync()
  -> PsaSnapshot
  -> PriorityEngine.Evaluate()
  -> DashboardState
  -> BusyBarRenderer.RenderAsync()
```

## Components

### IPsaDataProvider

The pluggable provider seam. One method, `GetSnapshotAsync`, returning a `PsaSnapshot`.
Implementations are registered in dependency injection, and the `Psa:Provider` configuration
setting selects which one runs at startup. See [Adding a PSA provider](/architecture) for how to
implement one.

### HaloPsaDataProvider

The built in provider. Authenticates to Halo's REST API through OAuth2 client credentials,
exchanging a client ID and secret for a bearer token that it refreshes as needed, then queries
Halo for the ticket data a `PsaSnapshot` needs.

### PsaSnapshot

The provider agnostic data contract: open ticket count, priority counts, SLA risk tickets,
unassigned ticket count, and VIP customer tickets. See [Adding a PSA provider](/architecture) for
what each field means to a new implementation.

### PriorityEngine

A pure function, `PsaSnapshot` in, `DashboardState` out. No I/O, no PSA or BUSY Bar dependency. It
evaluates a five level priority order, top to bottom, first match wins:

1. Rank 1 tickets present: critical mode.
2. SLA risk tickets present (breaching within the configured threshold): SLA warning mode.
3. VIP customer tickets present: critical mode, VIP variant.
4. Unassigned tickets present: normal mode with an unassigned count callout.
5. Otherwise: normal mode, standard dashboard.

### DashboardState

Which display mode is active, plus the specific values to render: counts, priority names when the
provider supplies them, and SLA timing.

### BusyBarRenderer

Turns a `DashboardState` into `DisplayDrawAsync` calls against the BUSY Bar, one method per
display mode. For critical mode it can render several pages (whichever condition triggered it,
then a rank 2 tier, then a rank 3 tier, then SLA risk tickets if any), picking the requested page
through its `cyclePage` parameter. See `PollingBackgroundService` below for how those pages get
shown in turn.

### PollingBackgroundService

A .NET `BackgroundService` driving the poll loop on `Psa:PollIntervalSeconds`. A failed poll (Halo
unreachable, authentication failure, BUSY Bar unreachable) is logged, and the service tries again
on the next interval rather than crashing the worker. When a poll produces critical mode with more
than one page to show, the service cycles through those pages every
`Dashboard:DisplayCycleSeconds` seconds for the rest of that poll interval, so every tier gets
screen time rather than only whichever one triggered the alert. Normal and SLA warning mode have
only one page each, so they are drawn once and left until the next poll. On shutdown, the service
clears the display so a container restart or deploy does not leave stale dashboard state on
screen.
```

- [ ] **Step 2: Modify `website/sidebars.ts`**

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docsSidebar: ['intro', 'configuration', 'architecture'],
};

export default sidebars;
```

- [ ] **Step 3: Modify `website/docusaurus.config.ts`'s footer**

Replace the `footer.links` array with:

```ts
      links: [
        {
          title: 'Docs',
          items: [
            {label: 'Getting Started', to: '/'},
            {label: 'Configuration', to: '/configuration'},
            {label: 'Architecture', to: '/architecture'},
          ],
        },
        {
          title: 'More',
          items: [
            {label: 'GitHub', href: 'https://github.com/homotechsual/psatool-busybar-agent'},
          ],
        },
      ],
```

- [ ] **Step 4: Modify `website/docs/intro.mdx`**

Insert this sentence directly after the pipeline code block (before the `## Quick start`
heading):

```mdx

Read [Architecture](/architecture) for what each of those pieces does.
```

Append to the `## Next` list:

```mdx
- [Architecture](/architecture): the poll, evaluate, render pipeline and its five level priority order.
```

- [ ] **Step 5: Build and verify**

Run (PowerShell, from `website/`):

```powershell
yarn build
Test-Path build/architecture/index.html
```

Expected: build succeeds, `Test-Path` returns `True`.

- [ ] **Step 6: Commit**

```bash
git add website/
git commit -m "Add Architecture page"
```

---

### Task 4: Adding a PSA provider page

**Files:**
- Create: `website/docs/providers.mdx`
- Modify: `website/docs/architecture.mdx`
- Modify: `website/sidebars.ts`
- Modify: `website/docusaurus.config.ts`
- Modify: `website/docs/intro.mdx`

**Interfaces:**
- Consumes: `docsSidebar: ['intro', 'configuration', 'architecture']` and the footer `'Docs'`
  group from Task 3. Also consumes the two temporary `[Adding a PSA provider](/architecture)`
  self-links Task 3 left in `architecture.mdx`; this task points both at `/providers` once that
  route exists.
- Produces: route `/providers`; `docsSidebar: ['intro', 'configuration', 'architecture', 'providers']`.

- [ ] **Step 1: Create `website/docs/providers.mdx`**

```mdx
---
sidebar_position: 4
---

# Adding a PSA provider

This app is built around one seam: `IPsaDataProvider`. Halo is the first implementation, not the
only one. Adding a new PSA means implementing one interface and registering it, not rewriting the
worker.

## The interface

```csharp
public interface IPsaDataProvider
{
    Task<PsaSnapshot> GetSnapshotAsync(CancellationToken cancellationToken);
}
```

One method. Whatever your PSA's API looks like, a new provider's job is to turn its ticket state
into a `PsaSnapshot`.

## What PsaSnapshot needs

- `OpenTicketCount`: total open tickets.
- `PriorityCounts`: ticket counts keyed by priority rank, where 1 is the most urgent tier your PSA
  defines and higher numbers are progressively less urgent. Use a numeric rank rather than your
  PSA's own label (Halo calls its tiers "Critical", "Urgent", "High"), so the contract stays
  usable by any future provider regardless of what it calls its own tiers.
- `PriorityNames` (optional): a display ready name per rank, if your PSA has one to offer, for
  example Halo's own tier names. This is purely a display enrichment kept separate from
  `PriorityCounts`. Leave a rank out, or leave the whole dictionary null, and the display falls
  back to a generic "P1", "P2" style label. If your PSA has no such concept, leave this null.
- `SlaRiskTickets`: every ticket with a known SLA timer, not filtered by any risk threshold,
  including tickets with days left and already breached tickets (a negative `MinutesRemaining`).
  Do not filter by threshold in your provider: `PriorityEngine` is the single place that applies
  the SLA risk threshold, so filtering here would duplicate, and risk diverging from, that logic.
- `UnassignedTicketCount`: open tickets with nobody assigned.
- `VipTickets`: open tickets belonging to a customer your PSA flags as VIP. There is no separate
  escalation concept; an open VIP customer ticket is the escalation.
- `OrganizationName` (optional): a display ready organisation or portal name, if your PSA can
  supply one, for example Halo's `portal_title`. Leave it null if your PSA has no such concept, or
  the lookup fails; the display falls back to the configured `Dashboard:HeaderText`.

## Registering the provider

`Program.cs` selects which provider runs based on the `Psa:Provider` configuration value:

```csharp
var psaProviderName = builder.Configuration.GetSection(PsaOptions.SectionName)["Provider"];
if (string.Equals(psaProviderName, "Halo", StringComparison.OrdinalIgnoreCase))
{
    builder.Services.AddHttpClient<IPsaDataProvider, HaloPsaDataProvider>((provider, client) =>
    {
        var options = provider.GetRequiredService<IOptions<HaloOptions>>().Value;
        client.BaseAddress = new Uri(options.BaseUrl);
        client.Timeout = TimeSpan.FromSeconds(30);
    });
}
else
{
    throw new InvalidOperationException($"Unknown Psa:Provider '{psaProviderName}'. Supported: Halo.");
}
```

Add a new branch alongside the Halo one, registering your implementation against
`IPsaDataProvider`, then select it by setting `Psa:Provider` to your provider's name.

## Authentication is your provider's own concern

`HaloAuthClient`'s OAuth2 client credentials flow is specific to Halo, not part of the
`IPsaDataProvider` contract. Your provider authenticates however its PSA requires, whether that is
OAuth2, an API key, or something else, and keeps that logic to itself.
```

- [ ] **Step 2: Fix the two forward-reference links in `website/docs/architecture.mdx`**

Change both occurrences of `[Adding a PSA provider](/architecture)` to
`[Adding a PSA provider](/providers)`, their correct target now that this page exists.

- [ ] **Step 3: Modify `website/sidebars.ts`**

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docsSidebar: ['intro', 'configuration', 'architecture', 'providers'],
};

export default sidebars;
```

- [ ] **Step 4: Modify `website/docusaurus.config.ts`'s footer**

Replace the `footer.links` array with:

```ts
      links: [
        {
          title: 'Docs',
          items: [
            {label: 'Getting Started', to: '/'},
            {label: 'Configuration', to: '/configuration'},
            {label: 'Architecture', to: '/architecture'},
            {label: 'Adding a PSA provider', to: '/providers'},
          ],
        },
        {
          title: 'More',
          items: [
            {label: 'GitHub', href: 'https://github.com/homotechsual/psatool-busybar-agent'},
          ],
        },
      ],
```

- [ ] **Step 5: Modify `website/docs/intro.mdx`**

Append to the `## Next` list:

```mdx
- [Adding a PSA provider](/providers): implement `IPsaDataProvider` for a PSA other than Halo.
```

- [ ] **Step 6: Build and verify**

Run (PowerShell, from `website/`):

```powershell
yarn build
Test-Path build/providers/index.html
```

Expected: build succeeds, `Test-Path` returns `True`. This also confirms the two links fixed in
Step 2 resolve correctly, since a broken link anywhere fails the whole build.

- [ ] **Step 7: Commit**

```bash
git add website/
git commit -m "Add Adding a PSA provider page"
```

---

### Task 5: Deployment page, final footer wiring, and the website README

**Files:**
- Create: `website/docs/deployment.mdx`
- Create: `website/README.md`
- Modify: `website/sidebars.ts`
- Modify: `website/docusaurus.config.ts`
- Modify: `website/docs/intro.mdx`
- Modify: `website/docs/configuration.mdx`

**Interfaces:**
- Consumes: `docsSidebar: ['intro', 'configuration', 'architecture', 'providers']` and the footer
  `'Docs'` group from Task 4; the `Psa:Halo:Scope` row in `configuration.mdx` still pointing at
  `/configuration` from Task 2's workaround.
- Produces: route `/deployment`; the final `docsSidebar` array
  (`['intro', 'configuration', 'architecture', 'providers', 'deployment']`) and final footer
  `'Docs'` group, matching the design spec exactly.

- [ ] **Step 1: Create `website/docs/deployment.mdx`**

```mdx
---
sidebar_position: 5
---

# Deployment

## Least privilege

- **Halo API client**: register a dedicated Halo API client (Configuration, then Integrations,
  then Halo API in Halo's admin UI) scoped to read only ticket and SLA access. Do not reuse an
  admin scoped client, and confirm `Psa:Halo:Scope` matches the specific read only scope your Halo
  tenant exposes for ticket data. The shipped default, `read:tickets`, is a reasonable starting
  point, but verify a scope of that name actually exists and is granted to your API client in your
  tenant.
- **Container**: the image runs as a non root user (`app`, the built in .NET container user) with
  no extra capabilities granted. `docker-compose.yml` additionally drops all Linux capabilities
  (`cap_drop: ALL`), sets `no-new-privileges`, and mounts the root filesystem read only with a
  `tmpfs` `/tmp`. Do not add `privileged: true` or extra `cap_add` entries to
  `docker-compose.yml`; nothing here needs them.

## Docker networking

The BUSY Bar presents as a USB Ethernet adapter with its own IP (for example `10.0.4.20`), so
reaching it from a container is a network routing question, not a USB passthrough one.

- **Linux Docker host**: `network_mode: host` (already set in `docker-compose.yml`) is simplest.
  The container shares the host's network stack and reaches the BUSY Bar exactly as the host does.
- **Windows (Docker Desktop, WSL2)**: `network_mode: host` does not expose host only adapters,
  like the BUSY Bar's USB Ethernet interface, into the WSL2 VM automatically; WSL2 has its own
  network namespace. Two working options:
  1. **Bridge the adapter into WSL2**: share the BUSY Bar's USB Ethernet adapter with the WSL2
     network through `wsl --shutdown` plus a `.wslconfig` `[wsl2] networkingMode=mirrored` setting
     (Windows 11 22H2 and later), which mirrors host network interfaces, including the USB
     Ethernet one, into WSL2 directly. `network_mode: host` then works as it does on Linux.
  2. **Run the worker directly on Windows instead of in Docker** for the BUSY Bar network hop.
     Skipping Docker entirely is simpler than fighting WSL2 networking if mirrored mode is not
     available on your Windows build.

  Verify actual reachability once deployed with `curl -sf http://<BusyBar address>/api/version`.
  Run this from the Docker host itself, or any machine on the same network as the BUSY Bar, not
  from inside the container; the runtime image ships no `curl`.

## Deploy

```bash
git clone https://github.com/homotechsual/psatool-busybar-agent.git
cd psatool-busybar-agent
cp .env.example .env
```

Fill in `HALO_CLIENT_ID`, `HALO_CLIENT_SECRET`, and `HALO_BASE_URL` in `.env`, then:

```bash
docker compose up -d --build
```
```

- [ ] **Step 2: Fix the `Psa:Halo:Scope` link in `website/docs/configuration.mdx`**

Change the row back to point at `/deployment`, its correct target:

```mdx
| `Psa:Halo:Scope` | `read:tickets` | OAuth2 scope requested. Set this to the minimum your Halo API client is actually granted; see Least privilege on the [Deployment](/deployment) page. |
```

- [ ] **Step 3: Modify `website/sidebars.ts`**

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docsSidebar: ['intro', 'configuration', 'architecture', 'providers', 'deployment'],
};

export default sidebars;
```

- [ ] **Step 4: Modify `website/docusaurus.config.ts`'s footer**

Replace the `footer.links` array with its final version:

```ts
      links: [
        {
          title: 'Docs',
          items: [
            {label: 'Getting Started', to: '/'},
            {label: 'Configuration', to: '/configuration'},
            {label: 'Architecture', to: '/architecture'},
            {label: 'Adding a PSA provider', to: '/providers'},
            {label: 'Deployment', to: '/deployment'},
          ],
        },
        {
          title: 'More',
          items: [
            {label: 'GitHub', href: 'https://github.com/homotechsual/psatool-busybar-agent'},
          ],
        },
      ],
```

- [ ] **Step 5: Modify `website/docs/intro.mdx`**

Insert this sentence directly after the `docker compose up -d --build` code block (still inside
the `## Quick start` section, before `## Next`):

```mdx

See [Deployment](/deployment) for least privilege guidance and Docker networking on Windows.
```

Append to the `## Next` list:

```mdx
- [Deployment](/deployment): least privilege setup and Docker networking.
```

- [ ] **Step 6: Create `website/README.md`**

```md
# website

The PSA BusyBar Agent docs site, built with [Docusaurus](https://docusaurus.io/). Deployed to
[psatool-busybar-agent.homotechsual.dev](https://psatool-busybar-agent.homotechsual.dev) via
Cloudflare Pages (see `../.github/workflows/deploy-docs.yml`).

Uses [Yarn](https://yarnpkg.com/) (pinned through Corepack, see `packageManager` in
`package.json`), not npm.

## Install

```bash
corepack enable
yarn install
```

## Local development

```bash
yarn start
```

Starts a local dev server and opens a browser window. Most changes reload live.

## Build

```bash
yarn build
```

Static output goes to `build/`.
```

- [ ] **Step 7: Build and verify**

Run (PowerShell, from `website/`):

```powershell
yarn build
Test-Path build/deployment/index.html
```

Expected: build succeeds, `Test-Path` returns `True`. This build now exercises every internal
link written across Tasks 1 through 5 (including the `Psa:Halo:Scope` row's link now correctly
resolving to `/deployment`), so a broken link anywhere in the site fails here.

- [ ] **Step 8: Local visual check**

Run (PowerShell, from `website/`):

```powershell
yarn start
```

Open the browser it launches (or `http://localhost:3000`) and confirm: the homepage is the
Getting Started page, the sidebar lists all five pages in order, the navbar's GitHub link and
logo render, the footer's Docs column lists all five pages, and the dark mode toggle in the
navbar switches themes. Stop the dev server (Ctrl+C) once confirmed.

- [ ] **Step 9: Commit**

```bash
git add website/
git commit -m "Add Deployment page, finish footer wiring, and add website README"
```

---

### Task 6: Cloudflare Pages deploy workflow

**Files:**
- Create: `.github/workflows/deploy-docs.yml`

**Interfaces:**
- Consumes: nothing from earlier tasks (this is a CI config file, not part of the Docusaurus
  build). Not runnable until this repo has a GitHub remote and a Cloudflare Pages project named
  `psatool-busybar-agent` exists with `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` secrets
  configured; that is expected, per the design spec.

- [ ] **Step 1: Create `.github/workflows/deploy-docs.yml`**

```yaml
name: Deploy Docs

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
  workflow_dispatch:

permissions:
  contents: write
  deployments: write
  pull-requests: write

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    if: ${{
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false)
      }}

    concurrency:
      group: ${{ github.workflow }}-${{ github.event.pull_request.head.ref || github.ref }}
      cancel-in-progress: true
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha || github.ref }}
          fetch-depth: 0

      - name: Remove built-in Yarn
        run: npm uninstall -g yarn

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v7
        with:
          node-version: 'current'
          cache: 'yarn'
          cache-dependency-path: website/yarn.lock

      - name: Install website dependencies
        working-directory: website
        run: yarn install --immutable

      - name: Build docs site
        working-directory: website
        run: yarn build

      # actions/checkout leaves the repo in a detached HEAD, so wrangler can't infer the
      # branch from git state, without --branch it falls back to the commit SHA, which
      # Cloudflare Pages treats as a non-production deploy even on pushes to main.
      - name: Publish to Cloudflare Pages
        id: cloudflare-pages-deploy
        uses: cloudflare/wrangler-action@v4
        with:
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: 'pages deploy website/build --project-name=psatool-busybar-agent --branch=${{ github.event.pull_request.head.ref || github.ref_name }}'
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve deployment URLs
        id: deployment-urls
        shell: bash
        env:
          DEPLOYMENT_URL: ${{ steps.cloudflare-pages-deploy.outputs.deployment-url }}
          ALIAS_URL: ${{ steps.cloudflare-pages-deploy.outputs.pages-deployment-alias-url }}
        run: |
          preview_url="$ALIAS_URL"
          if [ -z "$preview_url" ] || [ "$preview_url" = "undefined" ]; then
            preview_url="$DEPLOYMENT_URL"
          fi
          echo "deployment_url=$DEPLOYMENT_URL" >> "$GITHUB_OUTPUT"
          echo "preview_url=$preview_url" >> "$GITHUB_OUTPUT"

      - name: Write deployment summary
        shell: bash
        env:
          COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          ENVIRONMENT: ${{ steps.cloudflare-pages-deploy.outputs.pages-environment }}
          DEPLOYMENT_URL: ${{ steps.deployment-urls.outputs.deployment_url }}
          PREVIEW_URL: ${{ steps.deployment-urls.outputs.preview_url }}
        run: |
          {
            echo "## Deploying with Cloudflare Pages"
            echo
            echo "| Name | Result |"
            echo "| --- | --- |"
            echo "| Last commit | \`$COMMIT_SHA\` |"
            echo "| Deploy log | [View run]($RUN_URL) |"
            if [ "${{ github.event_name }}" = "push" ] && [ "${{ github.ref }}" = "refs/heads/main" ]; then
              echo "| Deployment URL | [$DEPLOYMENT_URL]($DEPLOYMENT_URL) |"
            else
              echo "| Preview URL | [$PREVIEW_URL]($PREVIEW_URL) |"
              echo "| Deployment URL | [$DEPLOYMENT_URL]($DEPLOYMENT_URL) |"
            fi
            echo "| Environment | $ENVIRONMENT |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Create commit comment
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        uses: peter-evans/commit-comment@v4
        with:
          body: |
            ### Successfully deployed to Cloudflare Pages!

            | Item | Value |
            | --- | --- |
            | **Latest commit** | ${{ github.event.pull_request.head.sha || github.sha }} |
            | **Deploy log** | ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }} |
            | **Deployment URL** | [${{ steps.cloudflare-pages-deploy.outputs.deployment-url }}](${{ steps.cloudflare-pages-deploy.outputs.deployment-url }}) |
            | **Environment** | ${{ steps.cloudflare-pages-deploy.outputs.pages-environment }} |

      - name: Create PR comment
        if: ${{ github.event_name == 'pull_request' }}
        uses: mshick/add-pr-comment@v3
        with:
          message: |
            ### Successfully deployed to Cloudflare Pages!

            | Item | Value |
            | --- | --- |
            | **Latest commit** | ${{ github.event.pull_request.head.sha || github.sha }} |
            | **Deploy log** | ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }} |
            | **Preview URL** | [${{ steps.cloudflare-pages-deploy.outputs.pages-deployment-alias-url || steps.cloudflare-pages-deploy.outputs.deployment-url }}](${{ steps.cloudflare-pages-deploy.outputs.pages-deployment-alias-url || steps.cloudflare-pages-deploy.outputs.deployment-url }}) |
            | **Environment** | ${{ steps.cloudflare-pages-deploy.outputs.pages-environment }} |
```

- [ ] **Step 2: Verify against the source workflow**

Run (PowerShell, from the repo root):

```powershell
git diff --no-index ../busybar-dotnet/.github/workflows/deploy-docs.yml .github/workflows/deploy-docs.yml
```

Expected diff, and nothing else: the `.NET` setup step, the `dotnet tool restore` step, and their
related comment lines are removed; `yarn build:full` becomes `yarn build`; the two comment lines
above the "Build docs site" step are removed (they referenced the now deleted API doc generation);
`--project-name=busybar-dotnet` becomes `--project-name=psatool-busybar-agent`. If any other line
differs, fix it to match the source workflow before continuing (everything else must be
byte-for-byte identical, since it is generic GitHub Actions plumbing, not project specific).

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/deploy-docs.yml
git commit -m "Add Cloudflare Pages deploy workflow for the docs site"
```

Note in the commit body: this workflow cannot succeed until this repository has a GitHub remote
and a Cloudflare Pages project named `psatool-busybar-agent` exists with
`CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` secrets configured.
